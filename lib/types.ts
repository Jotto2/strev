export type Activity = {
    id: string;
    title: string;
    category: string;
    description: string;
    createdBy: string;
    imageURL: string;
    madeByName: string;
    followedBy: string[];
    isPublic: boolean;
}

export type Post = {
    
}